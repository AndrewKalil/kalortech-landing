export const CLONE_COMMAND = `# Clone the knowledge center into ~/.claude
git clone https://github.com/kalortech/claude-auto-sufficiency ~/.claude`;

export const SYNC_COMMANDS = `# Push local changes to remote
./sync.sh push

# Pull latest from remote
./sync.sh pull`;
